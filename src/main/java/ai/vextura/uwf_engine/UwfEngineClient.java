// Code generated by vexctl sdk generate. DO NOT EDIT.
// Source: WorkflowEngine v1.2.3
package ai.vextura.uwf_engine;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.DeserializationFeature;
import ai.vextura.uwf_engine.models.*;
import ai.vextura.uwf_engine.runtime.*;

import java.util.List;
import java.util.Map;

/**
 * Generated SDK client for WorkflowEngine.
 * Auth, retry, and transport are handled automatically.
 *
 * All operations route through vex-gate. Pass VEX_GATE_URL as both
 * the M2MAuth URL and the endpoint URL — no separate auth service URL needed.
 *
 * Example:
 *   M2MAuth auth = new M2MAuth(
 *       System.getenv("VEX_GATE_URL"),
 *       System.getenv("VEX_CLIENT_ID"),
 *       System.getenv("VEX_CLIENT_SECRET")
 *   );
 *   UwfEngineClient client = UwfEngineClient.withEndpoint(
 *       System.getenv("VEX_GATE_URL"), auth
 *   );
 */
public class UwfEngineClient {

    private final Transport transport;
    private final AuthProvider auth;
    private static final ObjectMapper MAPPER = new ObjectMapper()
        .configure(DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES, false);

    /**
     * Create a client backed by RIP endpoint resolution.
     * @param ripUrl vex-config base URL — resolve at runtime via: vexctl rip resolve vex-config &lt;region&gt;
     */
    public UwfEngineClient(String ripUrl, AuthProvider auth) {
        this.transport = new Transport("WorkflowEngine", ripUrl);
        this.auth = auth;
    }

    /**
     * Create a client that calls endpoint directly, bypassing RIP.
     * Useful for local dev and smoke tests.
     */
    public static UwfEngineClient withEndpoint(String endpoint, AuthProvider auth) {
        UwfEngineClient c = new UwfEngineClient(endpoint, auth);
        c.transport.setFixedEndpoint(endpoint);
        return c;
    }

    /** ExecuteWorkflow — POST /workflow/executions/{workflowId} */
    public ExecutionResult executeWorkflow(ExecuteWorkflowInput req) {
        RetryPolicy policy = new RetryPolicy(3, 200, 5000, true);
        return RetryPolicy.execute(policy, () -> doExecuteWorkflow(req));
    }

    private ExecutionResult doExecuteWorkflow(ExecuteWorkflowInput req) {
        String token = auth.getToken();
        String path = "/workflow/executions/{workflowId}"
            .replace("{workflowId}", req.workflowId);
        String body = SdkException.toJson(MAPPER, req);
        var resp = transport.doRequest("POST", path, token, body);
        if (resp.statusCode() >= 400) {
            throw SdkException.fromResponse(resp);
        }
        return SdkException.parseJson(MAPPER, resp.body(), ExecutionResult.class);
    }

    /** AsyncExecuteWorkflow — POST /workflow/executions/{workflowId}/async */
    public AsyncExecuteResponse asyncExecuteWorkflow(AsyncExecuteInput req) {
        RetryPolicy policy = new RetryPolicy(3, 200, 5000, true);
        return RetryPolicy.execute(policy, () -> doAsyncExecuteWorkflow(req));
    }

    private AsyncExecuteResponse doAsyncExecuteWorkflow(AsyncExecuteInput req) {
        String token = auth.getToken();
        String path = "/workflow/executions/{workflowId}/async"
            .replace("{workflowId}", req.workflowId);
        String body = SdkException.toJson(MAPPER, req);
        var resp = transport.doRequest("POST", path, token, body);
        if (resp.statusCode() >= 400) {
            throw SdkException.fromResponse(resp);
        }
        return SdkException.parseJson(MAPPER, resp.body(), AsyncExecuteResponse.class);
    }

    /** GetExecutionStatus — GET /workflow/executions/{runId} */
    public ExecutionStatus getExecutionStatus(RunIdInput req) {
        RetryPolicy policy = new RetryPolicy(2, 500, 500, false);
        return RetryPolicy.execute(policy, () -> doGetExecutionStatus(req));
    }

    private ExecutionStatus doGetExecutionStatus(RunIdInput req) {
        String token = auth.getToken();
        String path = "/workflow/executions/{runId}"
            .replace("{runId}", req.runId);
        String body = SdkException.toJson(MAPPER, req);
        var resp = transport.doRequest("GET", path, token, body);
        if (resp.statusCode() >= 400) {
            throw SdkException.fromResponse(resp);
        }
        return SdkException.parseJson(MAPPER, resp.body(), ExecutionStatus.class);
    }

    /** GetExecutionResult — GET /workflow/executions/{runId}/result */
    public ExecutionResult getExecutionResult(RunIdInput req) {
        RetryPolicy policy = new RetryPolicy(2, 500, 500, false);
        return RetryPolicy.execute(policy, () -> doGetExecutionResult(req));
    }

    private ExecutionResult doGetExecutionResult(RunIdInput req) {
        String token = auth.getToken();
        String path = "/workflow/executions/{runId}/result"
            .replace("{runId}", req.runId);
        String body = SdkException.toJson(MAPPER, req);
        var resp = transport.doRequest("GET", path, token, body);
        if (resp.statusCode() >= 400) {
            throw SdkException.fromResponse(resp);
        }
        return SdkException.parseJson(MAPPER, resp.body(), ExecutionResult.class);
    }

    /** GetExecutionMetrics — GET /workflow/executions/{runId}/metrics */
    public ExecutionMetrics getExecutionMetrics(RunIdInput req) {
        return doGetExecutionMetrics(req);
    }

    private ExecutionMetrics doGetExecutionMetrics(RunIdInput req) {
        String token = auth.getToken();
        String path = "/workflow/executions/{runId}/metrics"
            .replace("{runId}", req.runId);
        String body = SdkException.toJson(MAPPER, req);
        var resp = transport.doRequest("GET", path, token, body);
        if (resp.statusCode() >= 400) {
            throw SdkException.fromResponse(resp);
        }
        return SdkException.parseJson(MAPPER, resp.body(), ExecutionMetrics.class);
    }

    /** CancelExecution — POST /workflow/executions/{runId}/cancel */
    public AckResponse cancelExecution(RunIdInput req) {
        return doCancelExecution(req);
    }

    private AckResponse doCancelExecution(RunIdInput req) {
        String token = auth.getToken();
        String path = "/workflow/executions/{runId}/cancel"
            .replace("{runId}", req.runId);
        String body = SdkException.toJson(MAPPER, req);
        var resp = transport.doRequest("POST", path, token, body);
        if (resp.statusCode() >= 400) {
            throw SdkException.fromResponse(resp);
        }
        return SdkException.parseJson(MAPPER, resp.body(), AckResponse.class);
    }

    /** PauseExecution — POST /workflow/executions/{runId}/pause */
    public AckResponse pauseExecution(RunIdInput req) {
        return doPauseExecution(req);
    }

    private AckResponse doPauseExecution(RunIdInput req) {
        String token = auth.getToken();
        String path = "/workflow/executions/{runId}/pause"
            .replace("{runId}", req.runId);
        String body = SdkException.toJson(MAPPER, req);
        var resp = transport.doRequest("POST", path, token, body);
        if (resp.statusCode() >= 400) {
            throw SdkException.fromResponse(resp);
        }
        return SdkException.parseJson(MAPPER, resp.body(), AckResponse.class);
    }

    /** ResumeExecution — POST /workflow/executions/{runId}/resume */
    public AckResponse resumeExecution(RunIdInput req) {
        return doResumeExecution(req);
    }

    private AckResponse doResumeExecution(RunIdInput req) {
        String token = auth.getToken();
        String path = "/workflow/executions/{runId}/resume"
            .replace("{runId}", req.runId);
        String body = SdkException.toJson(MAPPER, req);
        var resp = transport.doRequest("POST", path, token, body);
        if (resp.statusCode() >= 400) {
            throw SdkException.fromResponse(resp);
        }
        return SdkException.parseJson(MAPPER, resp.body(), AckResponse.class);
    }

    /** RetryExecution — POST /workflow/executions/{runId}/retry */
    public AsyncExecuteResponse retryExecution(RunIdInput req) {
        RetryPolicy policy = new RetryPolicy(2, 1000, 1000, false);
        return RetryPolicy.execute(policy, () -> doRetryExecution(req));
    }

    private AsyncExecuteResponse doRetryExecution(RunIdInput req) {
        String token = auth.getToken();
        String path = "/workflow/executions/{runId}/retry"
            .replace("{runId}", req.runId);
        String body = SdkException.toJson(MAPPER, req);
        var resp = transport.doRequest("POST", path, token, body);
        if (resp.statusCode() >= 400) {
            throw SdkException.fromResponse(resp);
        }
        return SdkException.parseJson(MAPPER, resp.body(), AsyncExecuteResponse.class);
    }

    /** ListExecutions — GET /workflow/executions */
    public ListExecutionsResponse listExecutions(ListExecutionsInput req) {
        return doListExecutions(req);
    }

    private ListExecutionsResponse doListExecutions(ListExecutionsInput req) {
        String token = auth.getToken();
        String path = "/workflow/executions";
        String body = SdkException.toJson(MAPPER, req);
        var resp = transport.doRequest("GET", path, token, body);
        if (resp.statusCode() >= 400) {
            throw SdkException.fromResponse(resp);
        }
        return SdkException.parseJson(MAPPER, resp.body(), ListExecutionsResponse.class);
    }

    /** ListWorkflows — GET /workflow/definitions */
    public ListWorkflowsResponse listWorkflows() {
        return doListWorkflows();
    }

    private ListWorkflowsResponse doListWorkflows() {
        String token = auth.getToken();
        String path = "/workflow/definitions";
        String body = "{}";
        var resp = transport.doRequest("GET", path, token, body);
        if (resp.statusCode() >= 400) {
            throw SdkException.fromResponse(resp);
        }
        return SdkException.parseJson(MAPPER, resp.body(), ListWorkflowsResponse.class);
    }

    /** GetWorkflow — GET /workflow/definitions/{workflowId} */
    public WorkflowInfo getWorkflow(WorkflowIdInput req) {
        return doGetWorkflow(req);
    }

    private WorkflowInfo doGetWorkflow(WorkflowIdInput req) {
        String token = auth.getToken();
        String path = "/workflow/definitions/{workflowId}"
            .replace("{workflowId}", req.workflowId);
        String body = SdkException.toJson(MAPPER, req);
        var resp = transport.doRequest("GET", path, token, body);
        if (resp.statusCode() >= 400) {
            throw SdkException.fromResponse(resp);
        }
        return SdkException.parseJson(MAPPER, resp.body(), WorkflowInfo.class);
    }

    /** HealthCheck — GET /workflow/health */
    public HealthResponse healthCheck() {
        return doHealthCheck();
    }

    private HealthResponse doHealthCheck() {
        String token = auth.getToken();
        String path = "/workflow/health";
        String body = "{}";
        var resp = transport.doRequest("GET", path, token, body);
        if (resp.statusCode() >= 400) {
            throw SdkException.fromResponse(resp);
        }
        return SdkException.parseJson(MAPPER, resp.body(), HealthResponse.class);
    }

}
